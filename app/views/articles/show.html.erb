<p id="notice"><%= notice %></p>

<div class="container">
  <div class="row">
    <div class="col-sm-6">
      <p>
  <strong><%= @article.title %></strong>
</p>

<p>
  <%= @article.content %>
</p>
      </div>
    <div class="col-sm-6">
      <%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit "Submit", class: "btn btn-primary" %>
  </p>
<% end %>
      </div>
  </div>
  <div class="row">
    <div class="col-sm-6">
       <strong>Comments</strong>
        <% @article.comments.each do |comment| %>
          <p>
            <b></b><%= comment.editor%></b><%='  '%><%=comment.body %>
          </p>
        <% end %>
      </div>
     </div>
</div>
    
    
